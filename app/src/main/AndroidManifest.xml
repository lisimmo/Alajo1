<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.akinola.alajo"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.READ_PROFILE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ActivitySplash"
            android:label="@string/title_activity_activity_splash"
            android:theme="@style/AppTheme.NoActionBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".ActivityMain"
            android:label="@string/app_name"
            android:theme="@style/AppTheme"
            >

        </activity>

        <activity
            android:name=".ActivityNewPayment"
            android:label="@string/title_activity_activity_new_payment"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".ActivityUserProfile"
            android:label="@string/title_activity_activity_user_profile"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login">
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:label="@string/title_activity_register"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".RegisterFormActivity"
            android:label="@string/title_activity_register_form"
            android:theme="@style/AppTheme">
        </activity>

    </application>

</manifest>
